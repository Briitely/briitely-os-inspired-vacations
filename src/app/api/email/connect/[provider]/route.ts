import {NextResponse}from"next/server";import crypto from"crypto";import{requireAuthenticatedUser}from"@/lib/supabase/auth";import{authorizationUrl,type EmailProvider}from"@/lib/email/oauth";
export async function GET(req:Request,{params}:{params:Promise<{provider:string}>}){const auth=await requireAuthenticatedUser();if(!auth)return NextResponse.redirect(new URL("/login",req.url));const{provider}=await params;if(provider!=="google"&&provider!=="microsoft")return NextResponse.json({error:"Unsupported provider"},{status:400});const state=crypto.randomBytes(24).toString("base64url");const origin=new URL(req.url).origin;const res=NextResponse.redirect(authorizationUrl(provider as EmailProvider,origin,state));res.cookies.set("email_oauth_state",`${auth.user.id}:${provider}:${state}`,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",maxAge:600,path:"/"});return res}
