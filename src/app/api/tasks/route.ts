import{NextResponse}from"next/server";import{getAuthenticatedUser}from"@/lib/supabase/auth";import{createClient}from"@/lib/supabase/server";
export async function GET(req:Request){const{user,error}=await getAuthenticatedUser();if(error||!user||!user.isActive)return NextResponse.json({error:"Unauthorized"},{status:401});const s=await createClient(),url=new URL(req.url),scope=url.searchParams.get("scope")??"mine";let q=s.from("travel_file_tasks").select("id,title,notes,assigned_to,due_date,status,completed_at,travel_file_id,assigned_profile:profiles!assigned_to(id,full_name),travel_file:travel_files!travel_file_id(id,client_name,destination,departure_date,stage)").neq("status","complete").order("due_date",{ascending:true,nullsFirst:false}).order("created_at",{ascending:true});if(scope!=="team")q=q.eq("assigned_to",user.id);const[{data,error:taskError},{data:profiles}]=await Promise.all([q,s.from("profiles").select("id,full_name").eq("is_active",true).order("full_name")]);if(taskError)return NextResponse.json({error:taskError.message},{status:500});return NextResponse.json({tasks:data??[],profiles:profiles??[],currentUserId:user.id})}
