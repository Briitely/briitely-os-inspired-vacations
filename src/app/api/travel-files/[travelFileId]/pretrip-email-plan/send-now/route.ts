import{NextResponse}from"next/server";import{getAuthenticatedUser}from"@/lib/supabase/auth";import{createClient}from"@/lib/supabase/server";import{POST as executeScheduledEmail}from"../execute/route";
export async function POST(request:Request,{params}:{params:Promise<{travelFileId:string}>}){const{user}=await getAuthenticatedUser();if(!user||!user.isActive||!["staff","admin","super_admin"].includes(user.role))return NextResponse.json({error:"Authentication required."},{status:401});const{travelFileId}=await params;const body=await request.json().catch(()=>null)as{email_code?:string}|null;if(!body?.email_code)return NextResponse.json({error:"Email code is required."},{status:400});const secret=process.env.PRETRIP_EMAIL_EXECUTION_SECRET?.trim();if(!secret)return NextResponse.json({error:"PRETRIP_EMAIL_EXECUTION_SECRET is not configured."},{status:500});const executionRequest=new Request(request.url,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${secret}`},body:JSON.stringify({email_code:body.email_code,force:true})});const response=await executeScheduledEmail(executionRequest,{params:Promise.resolve({travelFileId})});const data=await response.json().catch(()=>({}));if(!response.ok)return NextResponse.json(data,{status:response.status});return NextResponse.json(data)}