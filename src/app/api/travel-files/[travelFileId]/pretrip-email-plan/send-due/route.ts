import{NextResponse}from"next/server";import{getAuthenticatedUser}from"@/lib/supabase/auth";import{createClient}from"@/lib/supabase/server";
export async function POST(request:Request,{params}:{params:Promise<{travelFileId:string}>}){
 const{user}=await getAuthenticatedUser();if(!user||!user.isActive)return NextResponse.json({error:"Authentication required."},{status:401});if(!["staff","admin","super_admin"].includes(user.role))return NextResponse.json({error:"Staff access required."},{status:403});
 const{travelFileId}=await params,secret=process.env.PRETRIP_EMAIL_EXECUTION_SECRET?.trim();if(!secret)return NextResponse.json({error:"PRETRIP_EMAIL_EXECUTION_SECRET is not configured."},{status:500});
 const db=await createClient(),today=new Date().toISOString().slice(0,10);const{data:due,error}=await db.from("travel_pretrip_emails").select("email_code,email_name,scheduled_date").eq("travel_file_id",travelFileId).eq("enabled",true).is("sent_at",null).lte("scheduled_date",today).order("scheduled_date",{ascending:true});if(error)return NextResponse.json({error:error.message},{status:500});
 let sent=0,failed=0;const results=[] as Array<Record<string,unknown>>;for(const item of due??[]){try{const url=new URL(`/api/travel-files/${travelFileId}/pretrip-email-plan/execute`,request.url);const r=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${secret}`},body:JSON.stringify({email_code:item.email_code}),cache:"no-store"}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error??`Execution endpoint returned ${r.status}.`);sent++;results.push({emailCode:item.email_code,status:d.alreadySent?"already_sent":"sent",recipientCount:d.recipientCount??null})}catch(e){failed++;results.push({emailCode:item.email_code,status:"failed",error:e instanceof Error?e.message:"Unknown error"})}}
 return NextResponse.json({success:failed===0,date:today,due:(due??[]).length,sent,failed,results})
}