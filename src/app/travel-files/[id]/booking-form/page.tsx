import{redirect}from"next/navigation";import Link from"next/link";import{ArrowLeft}from"lucide-react";import{getAuthenticatedUser}from"@/lib/supabase/auth";import{createClient}from"@/lib/supabase/server";import{Button}from"@/components/core/ui/button";
const labels:Record<string,string>={firstName:"Legal first name",middleName:"Middle name",lastName:"Legal last name",preferredName:"Preferred name",dateOfBirth:"Date of birth",email:"Email",phone:"Phone",passportNumber:"Passport number",passportCountry:"Issuing country",passportIssueDate:"Passport issue date",passportExpiryDate:"Passport expiry date",name:"Name",relationship:"Relationship"};
function Field({label,value,changed,before}:{label:string;value:any;changed?:boolean;before?:any}){return <div className={`rounded-md border p-3 ${changed?"border-primary/50 bg-primary/10":"bg-background"}`}><div className="text-[10px] font-semibold uppercase tracking-wide text-muted-foreground">{label}</div><div className="mt-1 text-sm font-medium">{value||"—"}</div>{changed&&<div className="mt-1 text-xs text-muted-foreground">Previously: {before||"—"}</div>}</div>}
export default async function Page({params}:{params:Promise<{id:string}>}){const{user}=await getAuthenticatedUser();if(!user||!user.isActive)redirect("/login");const{id}=await params,db=await createClient();const{data:file}=await db.from("travel_files").select("id, client_name, destination").eq("id",id).maybeSingle();if(!file)return <main className="p-6">Travel File not found.</main>;const{data:s}=await db.from("booking_form_submissions").select("*").eq("travel_file_id",id).order("submitted_at",{ascending:false}).limit(1).maybeSingle();if(!s)return <main className="mx-auto max-w-5xl p-6"><Button variant="ghost" asChild><Link href={`/travel-files/${id}`}><ArrowLeft className="h-4 w-4"/>Back to Travel File</Link></Button><div className="mt-4 rounded-xl border bg-background p-6">No completed booking form yet.</div></main>;const snap=s.submitted_snapshot as any,changes=(s.changed_fields??[])as any[];const changed=(scope:string,travellerId:string|undefined,field:string)=>changes.find(c=>c.scope===scope&&c.travellerId===travellerId&&c.field===field);return <main className="mx-auto max-w-5xl space-y-4 p-6"><Button variant="ghost" asChild><Link href={`/travel-files/${id}`}><ArrowLeft className="h-4 w-4"/>Back to Travel File</Link></Button><div><div className="text-xs font-semibold uppercase tracking-wide text-primary">Completed Booking Form</div><h1 className="text-2xl font-semibold">{file.client_name} · {file.destination||"Travel"}</h1><p className="mt-1 text-sm text-muted-foreground">Submitted {new Date(s.submitted_at).toLocaleString("en-CA")} · <strong>{s.change_count} field{s.change_count===1?"":"s"} changed</strong></p></div><div className="rounded-lg border border-primary/30 bg-primary/10 p-4 text-sm">Highlighted fields were changed by the client. The previous value is shown beneath each change.</div>{(snap.travellers??[]).map((t:any,i:number)=><section key={t.id} className="rounded-xl border bg-[#fffefa] p-5"><h2 className="font-serif text-xl">{[t.firstName,t.lastName].filter(Boolean).join(" ")||`Traveller ${i+1}`}</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{Object.keys(labels).filter(k=>k!=="name"&&k!=="relationship").map(k=>{const c=changed("traveller",t.id,k);return <Field key={k} label={labels[k]} value={t[k]} changed={Boolean(c)} before={c?.before}/>})}</div></section>)}<section className="rounded-xl border bg-[#fffefa] p-5"><h2 className="font-serif text-xl">Emergency Contact</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{["name","relationship","phone","email"].map(k=>{const c=changed("emergency",undefined,k);return <Field key={k} label={labels[k]} value={snap.emergencyContact?.[k]} changed={Boolean(c)} before={c?.before}/>})}</div></section></main>}
