import{redirect}from"next/navigation";import Link from"next/link";import{ArrowLeft}from"lucide-react";import{getAuthenticatedUser}from"@/lib/supabase/auth";import{DashboardHeaderWrapper}from"@/components/app/dashboard-header-wrapper";import{SharedFooter}from"@/components/app/shared-footer";import{EmailTemplateEditor}from"@/components/app/email-template-editor";
export default async function EmailTemplatesPage(){const{user}=await getAuthenticatedUser();if(!user)redirect("/login");if(!["admin","super_admin"].includes(user.role))redirect("/admin");return <div className="min-h-screen bg-background"><DashboardHeaderWrapper fullName={user.fullName} email={user.email} role={user.role}/><main className="mx-auto max-w-6xl space-y-6 px-4 py-8 sm:px-6 lg:px-8"><Link href="/admin" className="inline-flex items-center gap-1.5 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="h-4 w-4"/>Back to Admin Settings</Link><div><h1 className="text-2xl font-bold">Email Templates</h1><p className="mt-1 text-muted-foreground">Manage the standard client emails sent from the dashboard and scheduled trip communications.</p></div><EmailTemplateEditor/></main><SharedFooter/></div>}
