import{redirect}from"next/navigation";import{getAuthenticatedUser}from"@/lib/supabase/auth";import{DashboardHeaderWrapper}from"@/components/app/dashboard-header-wrapper";import{SharedFooter}from"@/components/app/shared-footer";import{TaskDashboard}from"@/components/app/task-dashboard";
export default async function TasksPage(){const{user,error}=await getAuthenticatedUser();if(error||!user)redirect("/login?redirect=/tasks");return <div className="min-h-screen"><DashboardHeaderWrapper fullName={user.fullName} email={user.email} role={user.role}/><main className="mx-auto max-w-6xl px-4 py-6 sm:px-6 lg:px-8"><div className="mb-6"><div className="text-[10px] font-semibold uppercase tracking-[.16em] text-primary">Work Queue</div><h1 className="mt-1 text-3xl font-semibold tracking-tight">Tasks</h1><p className="mt-1 text-sm text-muted-foreground">Your operational to-dos across all Travel Files.</p></div><TaskDashboard/></main><SharedFooter maxWidth="max-w-6xl" label="Tasks"/></div>}
