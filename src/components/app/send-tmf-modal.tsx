"use client";
import {useState}from"react";import{useRouter}from"next/navigation";import{Loader2,X,FileText,AlertCircle}from"lucide-react";import{Button}from"@/components/core/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/core/ui/card";import{Badge}from"@/components/core/ui/badge";import{formatPhoneNumber}from"@/lib/format/phone";
interface Props{travelFileId:string;clientName:string;email:string;phone:string;destination:string|null;assignedAdvisorName:string|null;agreementType:string|null;tmfAmount:number|null;revisionsIncluded:number|null;agreementDate:string;isOpen:boolean;onClose:()=>void}
export function SendTmfModal({travelFileId,clientName,email,phone,destination,assignedAdvisorName,agreementType,tmfAmount,revisionsIncluded,agreementDate,isOpen,onClose}:Props){const router=useRouter();const[sending,setSending]=useState(false),[error,setError]=useState<string|null>(null),[sendBookingForm,setSendBookingForm]=useState<boolean|null>(null);if(!isOpen)return null;const isIvt=agreementType==="ivt",agreementLabel=agreementType==="all_inclusive"?"All-Inclusive":"IVT";async function handleSend(){if(sendBookingForm===null){setError("Please choose whether to send the Client Booking Form with this agreement.");return}setSending(true);setError(null);try{const res=await fetch(`/api/travel-files/${travelFileId}/send-tmf-agreement`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({sendBookingForm})});const data=await res.json();if(!res.ok){setError(data.error??"Failed to send Retainer Agreement.");return}if(data.result==="already_sent"){setError("This Retainer Agreement has already been sent.");return}onClose();router.refresh()}catch{setError("Something went wrong. Please try again.")}finally{setSending(false)}}function handleClose(){if(sending)return;setError(null);setSendBookingForm(null);onClose()}return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4"><Card className="flex max-h-[90vh] w-full max-w-lg flex-col overflow-hidden shadow-xl"><CardHeader className="flex-row items-center justify-between space-y-0 border-b"><CardTitle className="flex items-center gap-2"><FileText className="h-5 w-5 text-primary"/>Send Retainer Agreement</CardTitle><button type="button" onClick={handleClose}><X className="h-5 w-5"/></button></CardHeader><CardContent className="flex-1 space-y-5 overflow-y-auto p-6"><div className="rounded-lg border bg-muted/30 p-3"><p className="text-sm">Review the retainer agreement details below. Clicking Send will deliver the agreement to the client via Briitely.</p></div><div className="space-y-4"><h3 className="text-sm font-semibold">Agreement Details</h3><div className="grid gap-3 sm:grid-cols-2"><ReviewField label="Client Name" value={clientName}/><ReviewField label="Email" value={email}/><ReviewField label="Phone" value={formatPhoneNumber(phone)}/><ReviewField label="Destination" value={destination??"—"}/><ReviewField label="Assigned Advisor" value={assignedAdvisorName??"Unassigned"}/><ReviewField label="Agreement Type" value={agreementLabel}/><ReviewField label="Retainer Amount" value={tmfAmount!=null?`$${tmfAmount.toFixed(2)}`:"—"}/><ReviewField label="Agreement Date" value={agreementDate}/>{isIvt&&<ReviewField label="Revisions Included" value={revisionsIncluded!=null?String(revisionsIncluded):"—"}/>}</div></div><fieldset className="space-y-2 rounded-lg border p-4"><legend className="px-1 text-sm font-semibold">Send Client Booking Form with this agreement? *</legend><div className="flex gap-6"><label className="flex items-center gap-2 text-sm"><input type="radio" name="sendBookingForm" checked={sendBookingForm===true} onChange={()=>setSendBookingForm(true)} disabled={sending}/>Yes</label><label className="flex items-center gap-2 text-sm"><input type="radio" name="sendBookingForm" checked={sendBookingForm===false} onChange={()=>setSendBookingForm(false)} disabled={sending}/>No</label></div><p className="text-xs text-muted-foreground">Choose Yes when the client should complete or refresh their booking details for this trip.</p></fieldset><div className="flex items-center gap-2"><Badge variant="outline">{agreementLabel} Template</Badge><span className="text-xs text-muted-foreground">The correct Briitely template is selected automatically.</span></div>{error&&<div className="flex items-start gap-2 rounded-lg border border-destructive/30 bg-destructive/5 p-3"><AlertCircle className="mt-0.5 h-4 w-4 shrink-0 text-destructive"/><p className="text-sm text-destructive">{error}</p></div>}</CardContent><div className="flex justify-end gap-3 border-t p-4"><Button variant="outline" onClick={handleClose} disabled={sending}>Cancel</Button><Button onClick={handleSend} disabled={sending||sendBookingForm===null}>{sending&&<Loader2 className="h-4 w-4 animate-spin"/>}{sending?"Sending...":"Send Agreement"}</Button></div></Card></div>}
function ReviewField({label,value}:{label:string;value:string}){return <div className="space-y-0.5"><span className="text-xs text-muted-foreground">{label}</span><p className="text-sm font-medium">{value}</p></div>}
