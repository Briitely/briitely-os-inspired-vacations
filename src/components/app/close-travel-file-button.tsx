"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import{CheckCircle2,Loader2}from"lucide-react";import{Button}from"@/components/core/ui/button";
export function CloseTravelFileButton({travelFileId}:{travelFileId:string}){const router=useRouter(),[saving,setSaving]=useState(false),[error,setError]=useState<string|null>(null);async function closeFile(){if(!window.confirm("Close this Travel File? It will move to Past / Closed trips."))return;setSaving(true);setError(null);try{const r=await fetch(`/api/travel-files/${encodeURIComponent(travelFileId)}/post-trip-close`,{method:"POST"}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error??"Could not close the Travel File.");router.refresh()}catch(e){setError(e instanceof Error?e.message:"Could not close the Travel File.")}finally{setSaving(false)}}return <div className="space-y-2"><Button size="sm" onClick={closeFile} disabled={saving}>{saving?<Loader2 className="h-4 w-4 animate-spin"/>:<CheckCircle2 className="h-4 w-4"/>}Close Travel File</Button>{error&&<p className="max-w-sm text-xs text-destructive">{error}</p>}</div>}