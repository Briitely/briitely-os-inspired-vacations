"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import{Loader2,X}from"lucide-react";import{Button}from"@/components/core/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/core/ui/card";
interface Props{travelFileId:string;departureDate:string|null;returnDate:string|null;isOpen:boolean;onClose:()=>void}
export function CompleteBookingModal({travelFileId,isOpen,onClose}:Props){const router=useRouter();const[saving,setSaving]=useState(false),[error,setError]=useState<string|null>(null),[confirmed,setConfirmed]=useState(false);if(!isOpen)return null;async function submit(e:React.FormEvent){e.preventDefault();if(!confirmed)return setError("Please confirm that all reservations and outstanding tasks have been entered before completing this step.");setSaving(true);setError(null);try{const r=await fetch(`/api/travel-files/${travelFileId}/complete-booking`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({bookingSetupConfirmed:true})});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error??"Could not complete the booking.");onClose();router.refresh()}catch(e){setError(e instanceof Error?e.message:"Could not complete the booking.")}finally{setSaving(false)}}return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4"><Card className="w-full max-w-lg shadow-xl"><CardHeader className="flex-row items-center justify-between space-y-0 border-b"><CardTitle>Complete Booking</CardTitle><button type="button" onClick={onClose}><X className="h-5 w-5"/></button></CardHeader><form onSubmit={submit}><CardContent className="space-y-5 p-6"><p className="text-sm text-muted-foreground">Before completing the booking, make sure the Travel File reflects everything that has been booked and anything that still needs attention.</p><label className="flex items-start gap-3 rounded-lg border p-4 text-sm"><input className="mt-1 h-4 w-4" type="checkbox" checked={confirmed} onChange={e=>setConfirmed(e.target.checked)}/><span><strong>I have added all reservations with booking / confirmation numbers to Payments and created tasks for any items still outstanding.</strong></span></label>{error&&<p className="text-sm text-destructive">{error}</p>}</CardContent><div className="flex justify-end gap-3 border-t p-4"><Button type="button" variant="outline" onClick={onClose} disabled={saving}>Cancel</Button><Button type="submit" disabled={saving||!confirmed}>{saving&&<Loader2 className="h-4 w-4 animate-spin"/>}{saving?"Saving...":"Complete Booking"}</Button></div></form></Card></div>}
