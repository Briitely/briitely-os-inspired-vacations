"use client";
import{useEffect,useState}from"react";import{X}from"lucide-react";import{Button}from"@/components/core/ui/button";import{Input}from"@/components/core/ui/input";
type Payment={id:string;payment_type:string;description:string|null;supplier?:string|null;confirmation_number?:string|null;amount:number|null;currency:string|null;due_date:string|null;status:string|null;processed_at:string|null;card_last_four?:string|null};
export function PaymentEditorModal({travelFileId,payment,isOpen,onClose,onSaved}:{travelFileId:string;payment:Payment|null;isOpen:boolean;onClose:()=>void;onSaved:()=>void}){const[form,setForm]=useState({description:"",supplier:"",confirmationNumber:"",amount:"",currency:"CAD",dueDate:"",paymentType:"installment",status:"upcoming",cardLastFour:"",processedDate:""});const[busy,setBusy]=useState(false);const[error,setError]=useState("");useEffect(()=>{if(isOpen)setForm({description:payment?.description??"",supplier:payment?.supplier??"",confirmationNumber:payment?.confirmation_number??"",amount:payment?.amount==null?"":String(payment.amount),currency:payment?.currency??"CAD",dueDate:payment?.due_date??"",paymentType:payment?.payment_type??"installment",status:payment?.status??"upcoming",cardLastFour:payment?.card_last_four??"",processedDate:payment?.processed_at?.slice(0,10)??""})},[isOpen,payment]);if(!isOpen)return null;const field=(key:string,value:string)=>setForm(v=>({...v,[key]:value}));async function save(){setBusy(true);setError("");try{const r=await fetch(`/api/travel-files/${travelFileId}/payments`,{method:payment?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...form,paymentId:payment?.id,amount:form.amount===""?null:Number(form.amount)})}),d=await r.json();if(!r.ok)throw new Error(d.error||"Could not save payment.");onSaved();onClose()}catch(e){setError(e instanceof Error?e.message:"Could not save payment.")}finally{setBusy(false)}}async function remove(){if(!payment||!confirm("Delete this payment?"))return;setBusy(true);const r=await fetch(`/api/travel-files/${travelFileId}/payments`,{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({paymentId:payment.id})});setBusy(false);if(r.ok){onSaved();onClose()}else setError("Could not delete payment.")}
return <div className="fixed inset-0 z-[100] flex items-start justify-center bg-black/50 p-4 pt-16"><div className="max-h-[85vh] w-full max-w-2xl overflow-y-auto rounded-xl bg-background p-6 shadow-xl"><div className="flex justify-between"><div><h2 className="text-xl font-semibold">{payment?"Edit Payment":"Add Payment"}</h2><p className="text-sm text-muted-foreground">Record the reservation and payment details.</p></div><Button variant="ghost" size="sm" onClick={onClose}><X className="h-4 w-4"/></Button></div><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="space-y-1 md:col-span-2"><span className="text-sm font-medium">Payment / Reservation *</span><Input value={form.description} onChange={e=>field("description",e.target.value)}/></label><label className="space-y-1"><span className="text-sm font-medium">Supplier</span><Input value={form.supplier} onChange={e=>field("supplier",e.target.value)}/></label><label className="space-y-1"><span className="text-sm font-medium">Booking / Confirmation #</span><Input value={form.confirmationNumber} onChange={e=>field("confirmationNumber",e.target.value)}/></label><label className="space-y-1"><span className="text-sm font-medium">Amount</span><Input type="number" step="0.01" min="0" value={form.amount} onChange={e=>field("amount",e.target.value)}/></label><label className="space-y-1"><span className="text-sm font-medium">Currency</span><select className="h-10 w-full rounded-md border bg-background px-3" value={form.currency} onChange={e=>field("currency",e.target.value)}><option>CAD</option><option>USD</option><option>EUR</option><option>GBP</option></select></label><label className="space-y-1"><span className="text-sm font-medium">Due Date *</span><Input type="date" value={form.dueDate} onChange={e=>field("dueDate",e.target.value)}/></label><label className="space-y-1"><span className="text-sm font-medium">Payment Type</span><select className="h-10 w-full rounded-md border bg-background px-3" value={form.paymentType} onChange={e=>field("paymentType",e.target.value)}><option value="deposit">Deposit</option><option value="installment">Installment</option><option value="final">Final Payment</option><option value="other">Other</option></select></label><label className="space-y-1"><span className="text-sm font-medium">Status</span><select className="h-10 w-full rounded-md border bg-background px-3" value={form.status} onChange={e=>field("status",e.target.value)}><option value="upcoming">Upcoming</option><option value="ready_for_review">Ready for Review</option><option value="client_notified">Client Notified</option><option value="processing">Processing</option><option value="paid">Paid</option><option value="failed">Failed</option><option value="cancelled">Cancelled</option></select></label><label className="space-y-1"><span className="text-sm font-medium">Card Last 4</span><Input maxLength={4} value={form.cardLastFour} onChange={e=>field("cardLastFour",e.target.value.replace(/\D/g,"").slice(0,4))}/></label>{form.status==="paid"&&<label className="space-y-1"><span className="text-sm font-medium">Paid / Processed Date</span><Input type="date" value={form.processedDate} onChange={e=>field("processedDate",e.target.value)}/></label>}</div>{error&&<p className="mt-4 text-sm text-destructive">{error}</p>}<div className="mt-6 flex justify-between"><div>{payment&&<Button variant="destructive" onClick={remove} disabled={busy}>Delete</Button>}</div><div className="flex gap-2"><Button variant="outline" onClick={onClose} disabled={busy}>Cancel</Button><Button onClick={save} disabled={busy}>{busy?"Saving...":"Save Payment"}</Button></div></div></div></div>}
