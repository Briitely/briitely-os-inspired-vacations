"use client";
import{useMemo,useState}from"react";import Link from"next/link";import{Card,CardContent}from"@/components/core/ui/card";import{Badge}from"@/components/core/ui/badge";import{Button}from"@/components/core/ui/button";import{Search,Plane,AlertCircle,Clock,UserCheck,Inbox,CalendarClock,Plus}from"lucide-react";import type{DashboardTravelFile,TravelFileFilter}from"@/lib/travel/queries";import{formatStageLabel,formatStageBadgeVariant}from"@/lib/travel/stage-labels";import{formatDueOrWaiting,formatDateOnly,parseDateOnly,isOverdue}from"@/lib/travel/format";import{sortDashboardFiles}from"@/lib/travel/sort";import type{TravelStage}from"@/lib/travel/types";
interface Props{files:DashboardTravelFile[];currentUserId:string}const FILTER_TABS:{key:TravelFileFilter;label:string;icon:React.ReactNode}[]=[{key:"all_open",label:"All Open",icon:<Inbox className="h-4 w-4"/>},{key:"my_actions",label:"My Actions",icon:<UserCheck className="h-4 w-4"/>},{key:"waiting_on_client",label:"Waiting on Client",icon:<Clock className="h-4 w-4"/>},{key:"overdue",label:"Overdue",icon:<AlertCircle className="h-4 w-4"/>},{key:"departing_soon",label:"Departing Soon",icon:<CalendarClock className="h-4 w-4"/>}];const STAGE_OPTIONS:TravelStage[]=["new_inquiry","consult_booked","consultation_complete","tmf_sent","tmf_processing","planning_proposal","proposal_sent","negotiating","proposal_accepted","deposit_received","booking_confirmed","trip_plans_created","final_payment_pending","paid_in_full","docs_sent","travelling","travel_complete","lost_not_qualified"];
function responsible(file:DashboardTravelFile){const a=file.current_action;if(!a)return"—";if(a.responsible_type==="client")return"Client";if(a.responsible_type==="system")return"System";return file.responsible_user?.full_name??"Unassigned"}function responsibleKey(file:DashboardTravelFile){const a=file.current_action;if(!a)return"none";if(a.responsible_type==="client")return"client";if(a.responsible_type==="system")return"system";return a.responsible_user_id?`user:${a.responsible_user_id}`:"unassigned"}function actionLabel(file:DashboardTravelFile){const a=file.current_action;if(!a)return null;if(a.action_code==="send_tmf_agreement")return"Prepare Retainer Email";if(a.action_code==="collect_tmf_payment")return"Collect Retainer Payment";return a.title}
function applyFilter(files:DashboardTravelFile[],filter:TravelFileFilter,userId:string){switch(filter){case"all_open":return files;case"my_actions":return files.filter(f=>f.current_action?.responsible_type==="internal"&&f.current_action.responsible_user_id===userId);case"waiting_on_client":return files.filter(f=>f.current_action?.responsible_type==="client");case"overdue":return files.filter(f=>f.current_action?.due_at&&isOverdue(f.current_action.due_at)&&(f.current_action.status==="active"||f.current_action.status==="pending"));case"departing_soon":{const now=new Date(),end=new Date(now.getTime()+30*86400000);return files.filter(f=>f.departure_date&&parseDateOnly(f.departure_date)>=now&&parseDateOnly(f.departure_date)<=end)}default:return files}}
export function ClientJourneyDashboard({files,currentUserId}:Props){const[activeFilter,setActiveFilter]=useState<TravelFileFilter>("all_open"),[searchQuery,setSearchQuery]=useState(""),[stageFilter,setStageFilter]=useState<TravelStage|"all">("all"),[responsibleFilter,setResponsibleFilter]=useState("all");const responsibleOptions=useMemo(()=>{const map=new Map<string,string>();for(const f of files){const key=responsibleKey(f),label=responsible(f);if(key!=="none")map.set(key,label)}return Array.from(map.entries()).sort((a,b)=>a[1].localeCompare(b[1]))},[files]);const filtered=useMemo(()=>{let result=applyFilter(files,activeFilter,currentUserId);if(searchQuery.trim()){const q=searchQuery.toLowerCase();result=result.filter(f=>f.client_name.toLowerCase().includes(q)||(f.destination?.toLowerCase().includes(q)??false))}if(stageFilter!=="all")result=result.filter(f=>f.stage===stageFilter);if(responsibleFilter!=="all")result=result.filter(f=>responsibleKey(f)===responsibleFilter);return sortDashboardFiles(result)},[files,activeFilter,searchQuery,stageFilter,responsibleFilter,currentUserId]);if(!files.length)return <Card><CardContent className="flex flex-col items-center justify-center space-y-4 py-16"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-secondary"><Plane className="h-8 w-8 text-muted-foreground"/></div><p className="text-lg font-medium">No active Travel Files yet.</p><p className="max-w-sm text-center text-sm text-muted-foreground">Travel Files will appear here once inquiries come in. Create one from a customer&apos;s workspace.</p><Button asChild><Link href="/customers"><Plus className="h-4 w-4"/>Find a Customer</Link></Button></CardContent></Card>;
return <div className="space-y-4"><div className="flex flex-wrap items-center gap-2">{FILTER_TABS.map(tab=><Button key={tab.key} variant={activeFilter===tab.key?"default":"outline"} size="sm" onClick={()=>setActiveFilter(tab.key)}>{tab.icon}{tab.label}</Button>)}</div><div className="flex flex-wrap items-center gap-3"><div className="relative min-w-[200px] flex-1"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><input placeholder="Search client or destination..." value={searchQuery} onChange={e=>setSearchQuery(e.target.value)} className="w-full rounded-md border bg-background py-2 pl-9 pr-3 text-sm"/></div><select value={stageFilter} onChange={e=>setStageFilter(e.target.value as TravelStage|"all")} className="rounded-md border bg-background px-3 py-2 text-sm"><option value="all">All Stages</option>{STAGE_OPTIONS.map(s=><option key={s} value={s}>{formatStageLabel(s)}</option>)}</select><select value={responsibleFilter} onChange={e=>setResponsibleFilter(e.target.value)} className="rounded-md border bg-background px-3 py-2 text-sm"><option value="all">All Responsible</option>{responsibleOptions.map(([key,name])=><option key={key} value={key}>{name}</option>)}</select></div><Card><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b bg-muted/50"><th className="px-4 py-3 text-left font-medium text-muted-foreground">Client</th><th className="px-4 py-3 text-left font-medium text-muted-foreground">Trip</th><th className="px-4 py-3 text-left font-medium text-muted-foreground">Stage</th><th className="px-4 py-3 text-left font-medium text-muted-foreground">Next Action</th><th className="px-4 py-3 text-left font-medium text-muted-foreground">Responsible</th><th className="px-4 py-3 text-left font-medium text-muted-foreground">Due / Waiting</th><th className="px-4 py-3 text-left font-medium text-muted-foreground">Advisor</th></tr></thead><tbody className="divide-y">{!filtered.length&&<tr><td colSpan={7} className="px-4 py-12 text-center text-muted-foreground">No Travel Files match the current filters.</td></tr>}{filtered.map(file=>{const due=file.current_action?formatDueOrWaiting(file.current_action.due_at,file.current_action.waiting_since):"—",overdue=Boolean(file.current_action?.due_at&&isOverdue(file.current_action.due_at)),nextAction=actionLabel(file);return <tr key={file.id} className="hover:bg-muted/30"><td className="px-4 py-3"><Link href={`/travel-files/${file.id}`} className="font-medium text-primary hover:underline">{file.client_name}</Link></td><td className="px-4 py-3"><p>{file.destination||"Trip details pending"}</p>{(file.trip_type||file.departure_date||file.travel_timeframe)&&<p className="mt-0.5 text-xs text-muted-foreground">{file.trip_type}{file.trip_type&&file.departure_date&&" · "}{file.departure_date&&`Dep ${formatDateOnly(file.departure_date)}`}{!file.trip_type&&!file.departure_date&&file.travel_timeframe}</p>}</td><td className="px-4 py-3"><Badge variant={formatStageBadgeVariant(file.stage)}>{formatStageLabel(file.stage)}</Badge></td><td className="px-4 py-3">{nextAction??<span className="italic text-muted-foreground">No current action</span>}</td><td className="px-4 py-3">{responsible(file)}</td><td className="px-4 py-3"><span className={overdue?"font-medium text-destructive":""}>{due}</span></td><td className="px-4 py-3 text-muted-foreground">{file.assigned_advisor?.full_name??"—"}</td></tr>})}</tbody></table></div></Card></div>}
