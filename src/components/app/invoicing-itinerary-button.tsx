"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import{CheckCircle2,Loader2}from"lucide-react";import{Button}from"@/components/core/ui/button";
export function InvoicingItineraryButton({travelFileId}:{travelFileId:string}){const router=useRouter();const[saving,setSaving]=useState(false);const[error,setError]=useState<string|null>(null);async function complete(){setSaving(true);setError(null);try{const r=await fetch(`/api/travel-files/${encodeURIComponent(travelFileId)}/invoicing-itinerary`,{method:"POST"});const d=await r.json().catch(()=>({}));if(!r.ok){if(Array.isArray(d.incompleteTasks)&&d.incompleteTasks.length)throw new Error(`Complete these tasks first: ${d.incompleteTasks.join("; ")}.`);throw new Error(d.error??"Could not complete Invoicing & Itinerary.")}router.refresh()}catch(e){setError(e instanceof Error?e.message:"Could not complete Invoicing & Itinerary.")}finally{setSaving(false)}}return <div className="space-y-2"><Button size="sm" onClick={()=>void complete()} disabled={saving}>{saving?<Loader2 className="h-4 w-4 animate-spin"/>:<CheckCircle2 className="h-4 w-4"/>}Complete Invoicing & Itinerary</Button>{error&&<p className="max-w-sm text-xs text-destructive">{error}</p>}</div>}
